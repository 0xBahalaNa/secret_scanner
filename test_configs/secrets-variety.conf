# TEST DATA ONLY — all credentials in this file are fake.
# Never use real secrets in test files.

# This file tests the expanded regex-based detection patterns.
# Each section exercises a different pattern from the scanner.

# --- AWS Secret Access Key (40-char base64 after key assignment) ---
aws_secret_access_key = "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"

# --- AWS Session Token ---
aws_session_token = "FwoGZXIvYXdzEBYaDFAKETOKEN0000000EXAMPLEFAKETOKEN00000000EXAMPLETOKEN"

# --- API Key patterns ---
api_key = "fake-api-key-1234567890abcdef"
apikey: "another-fake-key-abcdef123456"
api-key = "yet-another-fake-key-000000"

# --- JWT Token (fake, structurally valid base64url segments) ---
auth_token = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIn0"

# --- Connection string with embedded credentials ---
database_url = "postgresql://admin:SuperSecret123@db.example.com:5432/mydb"

# --- Lines that should NOT trigger (false positive tests) ---
# This comment mentions password but has no assignment — should be silent.
# The secret to good code is readability — also should be silent.
log_level = "info"
region = "us-west-1"
